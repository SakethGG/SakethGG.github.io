---
---
<div class="placeholder">
  <strong>Sample entry.</strong> Edit or delete this file's frontmatter and body — see
  <code>src/content/</code> for the folder structure, one file per entry.
</div>

<style>
  .placeholder {
    border: 1px dashed var(--accent);
    border-radius: var(--radius);
    padding: 0.9rem 1.1rem;
    font-size: 0.9rem;
    color: var(--muted);
    margin-bottom: 2rem;
  }
  .placeholder strong {
    color: var(--accent);
  }
</style>
